import * as monaco from 'monaco-editor-core';
import { ThemeRegistrationResolved, ShikiInternal } from '@shikijs/core';

type Monaco = typeof monaco;
interface MonacoInferface {
    editor: Monaco['editor'];
    languages: Monaco['languages'];
}
interface MonacoTheme extends monaco.editor.IStandaloneThemeData {
}
declare function textmateThemeToMonacoTheme(theme: ThemeRegistrationResolved): MonacoTheme;
declare function shikiToMonaco(highlighter: ShikiInternal<any, any>, monaco: MonacoInferface): void;

export { type MonacoInferface, type MonacoTheme, shikiToMonaco, textmateThemeToMonacoTheme };
